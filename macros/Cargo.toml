[package]
name = "ui-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
quote = "1.0.47"
syn = { version = "3.0.3", default-features = false, features = [
  "derive",
  "parsing",
  "printing",
  "proc-macro",
] }
